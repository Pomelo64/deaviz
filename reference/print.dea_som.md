# Print a fitted DEA self-organizing map

Print a fitted DEA self-organizing map

## Usage

``` r
# S3 method for class 'dea_som'
print(x, ...)
```

## Arguments

- x:

  A `dea_som` object, as returned by
  [`compute_som`](https://pomelo64.github.io/deaviz/reference/compute_som.md).

- ...:

  Ignored.

## Value

`x`, invisibly.
