% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute-som.R
\name{compute_som}
\alias{compute_som}
\title{Fit a self-organizing map to a DEA problem}
\usage{
compute_som(
  x,
  rts = c("crs", "vrs"),
  xdim = 8,
  ydim = 8,
  rlen = 1000,
  seed = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{dea_data} object, or a data frame coerced by
\code{\link{as_dea_data}}.}

\item{rts}{Returns to scale, passed to \code{\link{compute_efficiency}}:
\code{"crs"} or \code{"vrs"}.}

\item{xdim, ydim}{Grid dimensions (positive integers; default \code{8} each).}

\item{rlen}{Number of training iterations (default \code{1000}).}

\item{seed}{Optional single number; if supplied, SOM training is made
reproducible without permanently changing the session's random state.}

\item{...}{Further arguments passed to \code{\link[kohonen]{som}}.}
}
\value{
An object of class \code{dea_som}: a list with the fitted
  \code{som} (a \code{kohonen} object), the DMU \code{labels}, per-DMU
  \code{efficiency}, per-node mean \code{node_efficiency} (\code{NA} for empty
  nodes), and the grid dimensions.
}
\description{
Trains a hexagonal self-organizing map (SOM) on the standardised input/output
data and records, for every node, the mean DEA efficiency of the DMUs mapped
to it. The result feeds \code{\link{plot_io_som}} (and other SOM views), so the
map is trained once and reused.
}
\examples{
\dontshow{if (requireNamespace("Benchmarking", quietly = TRUE) && requireNamespace("kohonen", quietly = TRUE)) withAutoprint(\{ # examplesIf}
df <- data.frame(
  dmu  = paste0("D", 1:12),
  i_x1 = runif(12, 2, 9), i_x2 = runif(12, 1, 5),
  o_y1 = runif(12, 3, 9), o_y2 = runif(12, 1, 4)
)
som <- compute_som(df, xdim = 4, ydim = 4, seed = 1)
som
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{plot_io_som}}, \code{\link[kohonen]{som}}
}
