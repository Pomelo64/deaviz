% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-io-mds.R
\name{plot_io_mds}
\alias{plot_io_mds}
\title{MDS co-plot of a DEA problem}
\usage{
plot_io_mds(
  x,
  transform = c("none", "ratio"),
  dist_method = c("euclidean", "manhattan", "maximum", "canberra", "minkowski"),
  mds_type = c("ratio", "interval", "ordinal", "mspline"),
  encode = "crs",
  point_size = 2,
  transparency = 0.7,
  fade = TRUE,
  labels = "none",
  max.overlaps.value = 10,
  subtitle = NULL,
  title = NULL,
  interactive = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{dea_data} object, or a data frame coerced by
\code{\link{as_dea_data}}.}

\item{transform}{What to compute distances on: \code{"none"} (the original
inputs and outputs) or \code{"ratio"} (all output/input ratios).}

\item{dist_method}{Distance measure, passed to \code{\link[stats]{dist}}.}

\item{mds_type}{Scaling level, passed to \code{\link[smacof]{smacofSym}}.}

\item{encode}{What to colour by: an efficiency model
(\code{"crs"}, \code{"vrs"}, \code{"drs"}, \code{"irs"}, \code{"fdh"},
\code{"add"}) or the name of one of the distance-space columns.}

\item{point_size}{Point size (default \code{2}).}

\item{transparency}{Point alpha in \code{[0, 1]} (default \code{0.7}).}

\item{fade}{Controls the single-DMU focus view. When one DMU is given to
\code{labels}, all other marks (and, for the network plots, everything
outside that DMU's sub-network; for the panel biplot, the other
trajectories) are faded so the chosen DMU stands out. \code{TRUE} (default)
uses a sensible fade level; \code{FALSE} disables it; a single number in
\code{[0, 1]} sets the alpha of the faded marks directly, where larger
values fade them less (e.g. \code{0.4} leaves them more visible).}

\item{labels}{Which DMUs to label: \code{"none"} (default), \code{"all"}
(label every DMU; \pkg{ggrepel} with \code{max.overlaps = Inf}),
\code{"max.overlaps"} (\pkg{ggrepel} using \code{max.overlaps.value}),
or the name/id of a single DMU to highlight just that one.
Use \code{"id"} to print each DMU's row number inside its own marker.}

\item{max.overlaps.value}{Passed to \pkg{ggrepel} when
\code{labels = "max.overlaps"} (default \code{10}); larger values keep
more crowded labels.}

\item{subtitle}{Optional subtitle shown beneath the title.}

\item{title}{Optional plot title. If \code{NULL} (default) one is generated.}

\item{interactive}{Logical; static \pkg{ggplot2} (default) or interactive
\pkg{plotly}.}

\item{...}{Additional arguments passed to \code{geom_point}.}
}
\value{
A \pkg{ggplot2} object, or a \pkg{plotly} object when
  \code{interactive = TRUE}.
}
\description{
Places the DMUs in two dimensions by multidimensional scaling (Adler and
Raveh, 2008) and colours them by a chosen quantity: a DEA efficiency score,
or one of the input/output variables (or their output-to-input ratios).
}
\examples{
\dontshow{if (all(vapply(c("Benchmarking", "smacof"), requireNamespace, logical(1), quietly = TRUE))) withAutoprint(\{ # examplesIf}
df <- data.frame(
  dmu  = paste0("D", 1:8),
  i_x1 = c(4, 7, 8, 4, 2, 5, 6, 3),
  i_x2 = c(3, 3, 1, 2, 4, 2, 5, 1),
  o_y  = c(5, 8, 6, 7, 3, 9, 4, 6)
)
plot_io_mds(df, encode = "crs")
\dontshow{\}) # examplesIf}
}
\references{
Adler, N., & Raveh, A. (2008). Presenting DEA graphically. \emph{Omega},
36(5), 715--729.

de Leeuw, J., & Mair, P. (2009). Multidimensional scaling using majorization:
SMACOF in R. \emph{Journal of Statistical Software}, 31(3), 1--30.
\doi{10.18637/jss.v031.i03}
}
\seealso{
\code{\link{compute_efficiency}}, \code{\link[smacof]{smacofSym}}
}
