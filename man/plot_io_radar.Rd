% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-io-radar.R
\name{plot_io_radar}
\alias{plot_io_radar}
\title{Radar plot of inputs and outputs}
\usage{
plot_io_radar(
  x,
  efficiency = c("crs", "vrs", "none"),
  orientation = "in",
  labels = "none",
  max.overlaps.value = 10,
  transparency = 0.7,
  fade = TRUE,
  subtitle = NULL,
  title = NULL,
  interactive = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{dea_data} object, or a data frame coerced by
\code{\link{as_dea_data}}.}

\item{efficiency}{Efficiency model used to colour the DMUs by
efficient/inefficient status: \code{"crs"} (default), \code{"vrs"} or
\code{"none"}.}

\item{orientation}{Measurement orientation for the efficiency scores.}

\item{labels}{DMU emphasis: \code{"none"} (default) or \code{"all"} draw
every DMU normally; the name/id of a single DMU outlines that DMU's
polygon in bold so it stands out. (Variable axis labels are always
shown.)}

\item{max.overlaps.value}{Accepted for API consistency; unused here
(default \code{10}).}

\item{transparency}{Opacity of the markers/areas, a single number in
\code{[0, 1]} (default \code{0.7}).}

\item{fade}{Controls the single-DMU focus view. When one DMU is given to
\code{labels}, all other marks (and, for the network plots, everything
outside that DMU's sub-network; for the panel biplot, the other
trajectories) are faded so the chosen DMU stands out. \code{TRUE} (default)
uses a sensible fade level; \code{FALSE} disables it; a single number in
\code{[0, 1]} sets the alpha of the faded marks directly, where larger
values fade them less (e.g. \code{0.4} leaves them more visible).}

\item{subtitle}{Optional subtitle shown beneath the title.}

\item{title}{Optional plot title.}

\item{interactive}{Logical; if \code{FALSE} (default) a static \pkg{ggplot2}
radar; if \code{TRUE} an interactive \pkg{plotly} \code{scatterpolar}.}

\item{...}{Additional arguments passed to \code{geom_polygon} (static mode).}
}
\value{
A \pkg{ggplot2} object, or a \pkg{plotly} object when
  \code{interactive = TRUE}.
}
\description{
Draws a radar (spider) plot with one axis per input and output, each min-max
scaled to \code{[0, 1]}, and one closed polygon per DMU with straight edges.
Polygons are coloured by efficient/inefficient status. A radial counterpart to
\code{\link{plot_io_parcoo}}.
}
\examples{
\dontshow{if (requireNamespace("Benchmarking", quietly = TRUE)) withAutoprint(\{ # examplesIf}
df <- data.frame(
  dmu  = paste0("D", 1:6),
  i_x1 = c(4, 7, 8, 4, 2, 5),
  i_x2 = c(3, 3, 1, 2, 4, 2),
  o_y  = c(5, 8, 6, 7, 3, 9)
)
plot_io_radar(df, efficiency = "crs")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{plot_io_parcoo}}, \code{\link{compute_efficiency}}
}
