% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-efficiency-distributions.R
\name{plot_efficiency_distributions}
\alias{plot_efficiency_distributions}
\title{Efficiency scores of the DMUs}
\usage{
plot_efficiency_distributions(
  x,
  rts = "crs",
  orientation = "in",
  type = c("histogram", "box", "bar"),
  bins = 30,
  labels = "none",
  max.overlaps.value = 10,
  transparency = 0.7,
  subtitle = NULL,
  title = NULL,
  interactive = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{dea_data} object, or a data frame coerced by
\code{\link{as_dea_data}}.}

\item{rts}{Returns-to-scale model(s) passed to
\code{\link{compute_efficiency}}; a character vector with any of
\code{"crs"} (default), \code{"vrs"}, \code{"drs"}, \code{"irs"},
\code{"fdh"}, \code{"add"}. Supplying more than one compares models.}

\item{orientation}{Measurement orientation passed to
\code{\link{compute_efficiency}}.}

\item{type}{\code{"histogram"} (default), \code{"box"}, or \code{"bar"}
(one bar per DMU).}

\item{bins}{Number of histogram bins (used when \code{type = "histogram"}).}

\item{labels}{Which DMUs to mark: \code{"none"} (default), \code{"all"}
(rug on histograms, jittered points on boxplots), or the name/id of a
single DMU to mark with a dashed line (histogram), a point (boxplot),
or a highlighted axis label (bar).}

\item{max.overlaps.value}{Accepted for API consistency; unused here
(default \code{10}).}

\item{transparency}{Opacity of the markers/areas, a single number in
\code{[0, 1]} (default \code{0.7}).}

\item{subtitle}{Optional subtitle shown beneath the title.}

\item{title}{Optional plot title.}

\item{interactive}{Logical; static \pkg{ggplot2} (default) or interactive
\pkg{plotly}.}

\item{...}{Additional arguments passed to the underlying geom.}
}
\value{
A \pkg{ggplot2} object, or a \pkg{plotly} object when
  \code{interactive = TRUE}.
}
\description{
Summarises the DEA efficiency scores of the decision-making units as a
histogram (default), a boxplot, or a per-DMU bar chart. More than one
returns-to-scale model can be requested at once: histograms are then stacked
vertically (one panel per model) and boxplots are drawn side by side, each
model in its own colour.
}
\examples{
\dontshow{if (requireNamespace("Benchmarking", quietly = TRUE)) withAutoprint(\{ # examplesIf}
df <- data.frame(
  dmu  = paste0("D", 1:6),
  i_x1 = c(4, 7, 8, 4, 2, 5),
  i_x2 = c(3, 3, 1, 2, 4, 2),
  o_y  = c(5, 8, 6, 7, 3, 9)
)
plot_efficiency_distributions(df, rts = "crs")
plot_efficiency_distributions(df, rts = c("crs", "vrs"), type = "box")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{compute_efficiency}}, \code{\link{plot_io_efficients}}
}
