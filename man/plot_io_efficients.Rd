% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-io-efficients.R
\name{plot_io_efficients}
\alias{plot_io_efficients}
\title{Counts of efficient and inefficient DMUs}
\usage{
plot_io_efficients(
  x,
  rts = c("crs", "vrs", "drs", "irs", "fdh", "add"),
  orientation = "in",
  tol = 1e-06,
  labels = TRUE,
  transparency = 0.7,
  subtitle = NULL,
  title = NULL,
  interactive = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{dea_data} object, or a data frame coerced by
\code{\link{as_dea_data}}.}

\item{rts}{Returns to scale passed to \code{\link{compute_efficiency}}.}

\item{orientation}{Measurement orientation passed to
\code{\link{compute_efficiency}}.}

\item{tol}{Tolerance for treating a score as efficient (default
\code{1e-6}).}

\item{labels}{Logical; if \code{TRUE} (default) the count and percentage are
printed on each bar.}

\item{transparency}{Opacity of the markers/areas, a single number in
\code{[0, 1]} (default \code{0.7}).}

\item{subtitle}{Optional subtitle shown beneath the title.}

\item{title}{Optional plot title.}

\item{interactive}{Logical; static \pkg{ggplot2} (default) or interactive
\pkg{plotly}.}

\item{...}{Additional arguments passed to \code{geom_col}.}
}
\value{
A \pkg{ggplot2} object, or a \pkg{plotly} object when
  \code{interactive = TRUE}.
}
\description{
Classifies the DMUs as efficient or inefficient (efficiency score equal to 1
within a tolerance) and draws a bar chart of the counts, annotated with the
percentage of DMUs in each group.
}
\examples{
\dontshow{if (requireNamespace("Benchmarking", quietly = TRUE)) withAutoprint(\{ # examplesIf}
df <- data.frame(
  dmu  = paste0("D", 1:6),
  i_x1 = c(4, 7, 8, 4, 2, 5),
  i_x2 = c(3, 3, 1, 2, 4, 2),
  o_y  = c(5, 8, 6, 7, 3, 9)
)
plot_io_efficients(df, rts = "crs")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{plot_efficiency_distributions}}, \code{\link{compute_efficiency}}
}
