% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-io-distributions.R
\name{plot_io_distributions}
\alias{plot_io_distributions}
\title{Distributions of inputs and outputs}
\usage{
plot_io_distributions(
  x,
  type = c("histogram", "box"),
  bins = 30,
  scale = TRUE,
  labels = "none",
  max.overlaps.value = 10,
  transparency = 0.7,
  x_angle = NULL,
  subtitle = NULL,
  title = NULL,
  interactive = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{dea_data} object, or a data frame coerced by
\code{\link{as_dea_data}}.}

\item{type}{\code{"histogram"} (default) or \code{"box"}.}

\item{bins}{Number of histogram bins (used when \code{type = "histogram"}).}

\item{scale}{Logical; if \code{TRUE} (default) variables are standardised
(z-scored) before plotting.}

\item{labels}{Which DMUs to mark: \code{"none"} (default), \code{"all"}
(a rug of every DMU on histograms, jittered points on boxplots), or the
name/id of a single DMU to mark it with a dashed vertical line
(histogram) or a point (boxplot).}

\item{max.overlaps.value}{Accepted for API consistency with the other
plots; unused here (default \code{10}).}

\item{transparency}{Opacity of the markers/areas, a single number in
\code{[0, 1]} (default \code{0.7}).}

\item{x_angle}{Angle in degrees for the x-axis tick labels, useful when
the input/output (or DMU) names on the x-axis are long and overlap.
\code{NULL} (default) keeps the plot's standard orientation; for
example \code{x_angle = 45} tilts the labels to make them readable.}

\item{subtitle}{Optional subtitle shown beneath the title.}

\item{title}{Optional plot title.}

\item{interactive}{Logical; if \code{FALSE} (default) returns a static
\pkg{ggplot2} object, if \code{TRUE} an interactive \pkg{plotly} object.}

\item{...}{Additional arguments passed to the underlying geom
(\code{geom_histogram} or \code{geom_boxplot}).}
}
\value{
A \pkg{ggplot2} object, or a \pkg{plotly} object when
  \code{interactive = TRUE}.
}
\description{
Shows the distribution of every input and output, either as faceted
histograms (default) or as boxplots. By default the variables are
standardised so they share a common scale.
}
\examples{
df <- data.frame(
  dmu  = paste0("D", 1:6),
  i_x1 = c(4, 7, 8, 4, 2, 5),
  i_x2 = c(3, 3, 1, 2, 4, 2),
  o_y  = c(5, 8, 6, 7, 3, 9)
)
plot_io_distributions(df)
plot_io_distributions(df, type = "box")

}
\seealso{
\code{\link{dea_data}}
}
