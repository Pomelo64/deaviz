% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-io-som.R
\name{plot_io_som}
\alias{plot_io_som}
\title{Plot a self-organizing map of DEA efficiency}
\usage{
plot_io_som(
  som,
  labels = "all",
  max.overlaps.value = 10,
  jitter_sd = 0.1,
  seed = NULL,
  transparency = 0.7,
  subtitle = NULL,
  title = "SOM: mean efficiency per node",
  ...
)
}
\arguments{
\item{som}{A \code{dea_som} object from \code{\link{compute_som}}, or a
\code{dea_data} object / data frame, in which case the map is fitted first.}

\item{labels}{Which DMUs to write on the map: \code{"all"} (default),
\code{"none"}, or the name/id of a single DMU to label only that one.}

\item{max.overlaps.value}{Accepted for API consistency; unused here
(default \code{10}).}

\item{jitter_sd}{Standard deviation of the random jitter applied to label
positions so co-located labels do not overlap (default \code{0.1}).}

\item{seed}{Optional single number making the label jitter reproducible
without changing the session's random state.}

\item{transparency}{Opacity of the markers/areas, a single number in
\code{[0, 1]} (default \code{0.7}).}

\item{subtitle}{Optional subtitle shown beneath the title.}

\item{title}{Plot title.}

\item{...}{When \code{som} is data rather than a \code{dea_som}, further
arguments passed to \code{\link{compute_som}}.}
}
\value{
The \code{dea_som} object, invisibly.
}
\description{
Draws the hexagonal SOM as a property map, colouring each node by the mean
efficiency of the DMUs assigned to it, and (optionally) labels the nodes with
the DMU names. Uses base graphics, so the plot is produced as a side effect
on the active device.
}
\examples{
\dontshow{if (requireNamespace("Benchmarking", quietly = TRUE) && requireNamespace("kohonen", quietly = TRUE)) withAutoprint(\{ # examplesIf}
df <- data.frame(
  dmu  = paste0("D", 1:12),
  i_x1 = runif(12, 2, 9), i_x2 = runif(12, 1, 5),
  o_y1 = runif(12, 3, 9), o_y2 = runif(12, 1, 4)
)
som <- compute_som(df, xdim = 4, ydim = 4, seed = 1)
plot_io_som(som)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{compute_som}}
}
